rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Server Admin SDK bypasses these rules.
    // Deny all client access until Auth is added.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
