rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Client SDK stays locked down. Active session + archive use Admin SDK.
    // users/{uid}.activeSessionId is the cross-device "current session" pointer.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
